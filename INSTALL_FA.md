# راهنمای نصب ClipNote v1.3.2 — فارسی کامل

> نسخه: 1.3.2 | سازندگان: Kourosh و Mehdi  
> ویکی: https://kourosh242.github.io/clipnote/ | دانلود: [clipnote-v1.3.2.zip](https://github.com/Kourosh242/clipnote/releases/download/v1.3.2/clipnote-v1.3.2.zip)

## ۱. چرا باید پشتیبان بگیرید؟

ClipNote کاملاً **محلی** است. یادداشت‌ها فقط در `chrome.storage.local` دستگاه شما هستند. اگر پوشه افزونه را جایگزین کنید یا Chrome را Reset کنید، بدون پشتیبان داده‌ها از بین می‌روند.

### نحوه پشتیبان‌گیری

1. افزونه را باز کنید → **تنظیمات / Settings**
2. بخش **Import / Export** → **Export JSON**
3. فایل را در جایی **خارج از پوشه افزونه** ذخیره کنید (مثلاً Desktop یا Google Drive)

### بازگردانی

- همان بخش → **Import JSON** → فایل را انتخاب کنید
- ورود داده‌ها **ادغام** می‌شود، اطلاعات فعلی حذف نمی‌شود (از v1.2.6 به بعد)

---

## ۲. نصب نسخه ۱.۳.۲

### دانلود

- فقط از [GitHub Releases](https://github.com/Kourosh242/clipnote/releases/tag/v1.3.2) فایل `clipnote-v1.3.2.zip` را بگیرید
- فایل ZIP در شاخه main نیست، فقط به عنوان Asset ریلیز منتشر می‌شود

### استخراج

- ZIP را Extract کنید
- باید یک پوشه به نام `clipnote` داشته باشید که داخلش `manifest.json` باشد
- اگر پوشه داخلی `clipnote_v1.3.2` است، آن را به `clipnote` تغییر نام دهید یا محتویات داخلی را مستقیماً استفاده کنید

### بارگذاری در Chrome

1. آدرس `chrome://extensions/` را باز کنید
2. گوشه بالا راست، **Developer mode** را روشن کنید
3. دکمه **Load unpacked** را بزنید
4. پوشه `clipnote` که `manifest.json` داخلش است را انتخاب کنید
5. افزونه باید با آیکون ClipNote ظاهر شود

### به‌روزرسانی از نسخه قبلی

1. **حتماً Export JSON بگیرید**
2. پوشه قدیمی `clipnote` را با پوشه جدید جایگزین کنید (یا پوشه جدید را در جای جدید Extract کنید)
3. در صفحه `chrome://extensions/` روی دکمه **Reload** (↻) افزونه ClipNote کلیک کنید
4. اگر یادداشت‌ها نیامدند، Import JSON را انجام دهید

---

## ۳. استفاده اولیه

### ذخیره سریع از وب

1. در هر صفحه وب، متنی را انتخاب کنید
2. راست‌کلیک → **Save to ClipNote**
3. اعلان "Saved successfully" می‌آید
4. در پاپ‌آپ، بنر "ذخیره سریع" را می‌بینید که نام فضا و عنوان یادداشت را نشان می‌دهد

### پاپ‌آپ (Ctrl+Shift+N)

- **جستجو:** عنوان، متن، برچسب، دسته‌بندی
- **فیلتر فضا:** همه یا یک فضای کاری خاص
- **نما:** Overview (گروه‌بندی زمانی) / Timeline
- **فیلتر:** همه / پین‌شده / علاقه‌مندی
- **یادداشت جدید:** دکمه ＋ → عنوان، محتوا، فضا، دسته، برچسب، رنگ، پین/علاقه‌مندی

### مدیریت کامل (Options)

- کلیک روی **Open Manager** در پاپ‌آپ یا از `chrome://extensions/` → Details → Extension options
- **سایدبار:** همه یادداشت‌ها، فضاهای کاری، دسته‌بندی‌ها، برچسب‌ها
- **ویرایشگر:** Markdown، پیش‌نمایش، Split View، برچسب‌های سریع، پیشنهاد هوشمند، قفل
- **تنظیمات:** تم (آبی/سبز/بنفش/نارنجی/دارک پرو)، حالت تیره، فونت، انیمیشن، ذخیره خودکار، زبان (فارسی/English)

---

## ۴. قفل و بازیابی

### قفل کردن

1. یادداشت را باز کنید → بخش **محافظت / Protection**
2. **قفل / Lock** → نوع: رمز یا پین ۴ رقمی → وارد کنید → ذخیره
3. یادداشت قفل می‌شود

### سوال بازیابی (اختیاری اما پیشنهادی)

1. در همان بخش، **تنظیم سوال بازیابی**
2. سوال و جواب را وارد کنید → ذخیره
3. اگر رمز را فراموش کردید، در صفحه باز کردن، **بازیابی رمز** را بزنید

> **هشدار:** قفل، رمزنگاری نیست! فقط دسترسی در رابط را محدود می‌کند. فایل پشتیبان JSON می‌تواند محتوای قفل‌شده را داشته باشد.

---

## ۵. Markdown

- `# عنوان` تا `######`
- `**bold**`, `*italic*`, `~~strike~~`
- `` `inline code` `` و ```` ```code block``` ````
- `[لینک](https://example.com)` — فقط https/mailto مجاز است، javascript: مسدود است
- `![alt](https://...)` برای تصویر
- لیست‌های تو در تو با `-` یا `1.` و تورفتگی ۲ فاصله
- جدول با `| ستون | ستون |`

دکمه **Copy** در هر code block وجود دارد.

---

## ۶. عیب‌یابی

| مشکل | راه‌حل |
|------|--------|
| یادداشت‌ها بعد از به‌روزرسانی نیست | Import JSON را بزنید؛ بررسی کنید پوشه درست Load شده |
| پاپ‌آپ خالی است | فضای کاری فیلتر را روی "همه" بگذارید؛ جستجو را پاک کنید |
| قفل باز نمی‌شود | زبان صفحه‌کلید را چک کنید؛ از بازیابی استفاده کنید |
| Markdown خراب است | مطمئن شوید ``` را درست بسته‌اید |
| اعلان به‌روزرسانی نمی‌آید | بررسی کنید اینترنت وصل است؛ افزونه به api.github.com دسترسی دارد |

---

## ۷. حریم خصوصی

- ۱۰۰٪ محلی، بدون سرور
- بدون ردیابی
- فقط یک درخواست به `api.github.com/repos/Kourosh242/clipnote/releases/latest` برای چک نسخه (هر ۶ ساعت)

---

## ۸. لینک‌های مفید

- [ویکی آنلاین دو زبانه](https://kourosh242.github.io/clipnote/)
- [CHANGELOG](CHANGELOG.md)
- [WIKI.md](WIKI.md)
- [گزارش باگ](https://github.com/Kourosh242/clipnote/issues)

ساخته‌شده با ❤️ — اگر مفید بود، یک ⭐ در GitHub بدهید!
